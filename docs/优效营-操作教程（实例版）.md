# 优效营（uxyy.cn）系统操作教程

> 实例驱动学习 - 从零开始掌握系统全部功能

---

## 📖 教程概述

本教程通过一个**虚拟企业案例**，带您逐步体验优效营系统的核心功能。

### 🎯 学习目标

1. 完成系统注册与登录
2. 掌握CRM客户管理
3. 学会进销存操作
4. 理解财务记账流程
5. 使用OA办公功能
6. 体验AI智能功能

### 🏢 案例背景

**企业名称**：小明便利店  
**行业**：零售/批发  
**规模**：3家分店，5名员工  
**业务场景**：日常销售、采购补货、会员管理、财务记账

### 📋 模拟数据与实例速查（小明便利店）

以下数值在全文中多次出现，**便于按同一套故事做练习**；复制到表单时请按您环境中是否已占用略作改名（例如手机、商品编码）。

**主体**

| 项目 | 模拟取值 | 常用于 |
|------|-----------|--------|
| 企业名称 | `小明便利店` | 注册 |
| 登录手机 | `13800138000`（注册勿与已存在冲突） | §1.1 / seed |
| 登录密码 | `Dev12345!` | §1.1 |
| 店长/默认用户 ID | 在「用户资料」中查看当前账号 **用户 ID** | 商机负责人、归属销售 |

**CRM（客户与商机）**

| 项目 | 模拟取值 | 章节 |
|------|-----------|------|
| 客户名称 | `王老板` | §2.1～§2.5、§3.3 |
| 联系人 / 电话 / 地址 | `张三`、`13900139000`、`浙江省杭州市余杭区` | §2.1 |
| 客户（练习用） | `刘老板` | 任务1 |
| 客户分类名称 | `批发大客户`；描述示例：`月采饮料类稳定、议价空间大` | §2.2 |
| 商机名称 | `批量采购饮料` | §2.3 |
| 商机预计金额 | `5000` 元；预计成交日 `2026-06-15` | §2.3 |
| 跟进内容示例 | `已沟通批量采购事宜，客户表示感兴趣` | §2.5 |

**进销存（供应商 / 商品 / 采购）**

| 项目 | 模拟取值 | 章节 |
|------|-----------|------|
| 供应商 A | 名称 `旺旺食品`；联系人 `张三`；电话 `13900001111`；地址 `杭州市余杭区某某路100号` | §3.0、§3.2 |
| 供应商 B（练习） | `农夫山泉` | 任务2 |
| 商品「可乐」 | 编码 `SKU-COLA`；名称 `可乐`；规格 `500ml`；单位 `箱`；销售单价 `55`；成本价 `40`；最低/最高库存 `20` / `500` | §3.1、§3.2、§3.3 |
| 商品「矿泉水」（练习） | 编码 `SKU-WATER-550`；名称 `550ml 矿泉水`；单位 `箱`；销售单价 `36` 等 | 任务1、任务2 |
| 商品「示例 B」（与界面占位一致，可选） | 编码 `P001`；名称 `六角螺栓 M8×30`；规格 `M8×30 8.8级`；单位 `件`；销售单价 `12.50`；成本价 `8` | §3.1 |
| 采购单行 | 供应商 `旺旺食品`；商品 `可乐`；数量 `50`；行单价 `45`；备注 `首批铺货` | §3.2 |
| 销售单行 | 客户 `王老板`；商品 `可乐`；数量 `10`；单价 `55` | §3.3 |
| 仓库「主仓库」 | 名称 `主仓库`；编码 `WH-MAIN`（可选）；地址示例 `小明便利店总店 …`；**勾选**「设为默认仓库」 | §3.4.1 |
| 仓库「分店」（练习） | 名称 `分店A`；编码 `WH-A`（可选）；**不勾选**默认 | §3.4.1 |

**财务 / OA（示意，非必然与真账一致）**

| 项目 | 模拟取值 | 章节 |
|------|-----------|------|
| 发票不含税金额 / 税 / 价税合计 | `2250` / `292.5` / `2542.5` | §4.1～§4.2 |
| 税务申报所属期 | `2026-05`；增值税示例：销项 `6500`、进项 `3900`、应缴 `2600` | §4.4 |
| 考勤 | `2026-05-08 08:55`、地点 `小明便利店总店` | §5.1 |
| 请假 | 事假、`2026-05-10`～`2026-05-11`、原因 `家中有事` | §5.2 |
| 报销 | 差旅费、`500` 元 | §5.4 |

> **说明**：标为「示意」的财务、OA、预警表格式数据，**仅用于理解界面与流程**；您环境中的凭证号、检测结果以实际为准。

### 教程与当前版本（重要）

本教程**以实例故事串联功能**，其中部分章节描述的是**完整产品愿景**或**示意数据**；与仓库中 **MVP 前端 + 后端**一致、且可按步骤操作的能力，目前主要包括（但不限于）：

| 模块 | 已在 Web 端较好对齐的示例 |
|------|---------------------------|
| 入门 | 注册 / 登录（验证码在开发环境可填占位） |
| CRM | 客户列表与新建、**商机列表 / 新建编辑 / 详情页**、**商机详情中的 AI 话术推荐**、**跟进记录（先选客户再新增）**、客户分类等 |
| 其他 | **进销存**：**供应商管理（§3.0）**、**商品管理 / SKU（§3.1）**、**采购订单**（草稿 → 提交 → 审批 → 入库闭环见 **§3.2**）、**仓库管理 → 新建仓库（§3.4.1）** 等以**实际侧边栏与首页卡片为准**；财务、OA、部分 AI 大屏等同理，若菜单或按钮不一致，请以界面为准或查阅 API 文档 |

阅读时请关注文中 **「说明」「以实际为准」** 提示，避免将示意截图数据当作您环境中的真实结果。

---

## 第一章：系统入门

### 1.1 注册与登录

**步骤1：访问系统**
```
地址：http://localhost:3000
```

**步骤2：注册账号**
1. 点击「注册」或打开注册页 `/register`
2. 填写**手机号**（示例：`13800138000`，请勿与已注册账号冲突）
3. **验证码**：当前前端提示为「开发环境输入任意值」；请按注册页说明通过校验即可（具体规则以 `register` 表单校验为准）
4. 设置**密码**（须满足「至少 8 位」等规则，示例：`Dev12345!`）
5. **企业名称**：`小明便利店`
6. 点击「注册」

**步骤3：登录系统**
1. 打开 `/login`，输入手机号和密码
2. 点击「登录」

> **本地开发种子账号（可选）**：若您运行过后端 `seed`，默认示例用户一般为手机 `13800138000`、密码 `Dev12345!`（亦可通过环境变量 `SEED_DEV_PHONE` / `SEED_DEV_PASSWORD` 覆盖）。与教程自拟注册不冲突，任选其一即可登录体验。

### 1.2 首页概览

登录后请进入 **工作台 / Dashboard**，侧边栏通常包含：首页、客户管理、进销存、财务、OA 办公、AI 智能等（**以您当前版本界面为准**）。

**模拟看板（教学示意，非真实运行结果）**：若首页有经营概览类卡片，可自行对照下列数字理解「小店一天长什么样」——

| 指标（示例） | 模拟值 |
|--------------|--------|
| 今日销售额 | ¥8,260 |
| 待处理采购单 | 2 张 |
| 待跟进客户 | 5 人（含「王老板」） |
| 库存预警 SKU | 1 个（如矿泉水低于下限） |

首页卡片与看板指标可能随迭代变化；**若界面与上表不一致，以您当前环境为准**。

---

## 第二章：客户管理

### 2.1 创建客户

**场景**：小明便利店新来了一位大客户「王老板」，需要录入系统

**操作步骤**：
1. 点击左侧菜单「客户管理」
2. 在客户管理首页，点击「客户列表」卡片
3. 进入客户列表页面后，点击右上角「新建」按钮
4. 填写信息（完整字段）：
   - **客户名称**（必填）：`王老板`
   - **联系人**（可选）：`张三`
   - **电话**（可选）：`13900139000`
   - **地址**（可选）：`浙江省杭州市余杭区`
   - **客户类型**（可选）：`企业客户`（企业客户/个人客户）
   - **客户等级**（可选）：`VIP`（VIP/普通/潜在）
   - **行业**（可选）：`零售`
   - **标签**（可选）：`重点客户`, `货到付款`
   - **来源**（可选）：`手动录入`（手动录入/导入/微信/其他）
   - **归属销售 ID**（可选）：填写负责跟进的销售员在用户体系中的**数字用户 ID**（与「设置 / 用户资料」中显示的「用户 ID」一致）；不填表示暂不指定归属销售
   - **信用额度**（可选）：`50000`
   - **备注**（可选）：`重点客户，货到付款`
5. 点击「创建」

**效果**：客户档案创建成功，可在列表中查看与继续维护。

> **💡 字段说明**：
> - `客户类型`：区分企业客户和个人客户，影响后续业务处理
> - `客户等级`：VIP客户可享受会员价和专属服务
> - `标签`：用于快速分类和筛选客户
> - `信用额度`：设置客户的赊账上限，超限时系统自动预警
> - **归属销售 ID**：对应数据库中的内部用户主键（`users.id`），不是手机号；给自己归类时可填当前账号在「用户资料」里看到的用户 ID
>
> **💡 关于会员积分**：当前 MVP 中，**新建客户不会自动创建「会员档案」记录**；会员等级与积分在「会员管理」等能力中单独维护。教程里说的「从 0 分起步」指业务含义上的新客，若需可查可兑的积分，请在会员相关菜单中为客户建档或积分入账。

### 2.2 客户分类

填写下列步骤时，可与文首 **「模拟数据速查」** 表中 CRM 区「客户分类」一行对照。

**场景**：小明便利店希望把客户按业务维度打上统一标签（例如「批发大客户」），便于列表筛选与后续运营。

**操作步骤**：
1. 点击左侧菜单「客户管理」
2. 在客户管理首页，点击「**客户分类**」卡片（说明文字为「按状态、行业、区域等维度分类客户」）
3. 进入页面 `客户分类` 后，点击右上角「**新增分类**」按钮（带加号图标）
4. 在弹窗「**新增客户分类**」中填写（与当前前端表单一致）：
   - **分类名称**（必填）：如 `批发大客户`
   - **类型**（可选）：默认「**自定义**」；亦可选用「成交状态」「行业」「区域」（与后端枚举、列表中的类型展示一致）
   - **描述**（可选）：如 `月采饮料类稳定、议价空间大`
   - **颜色**（可选）：取色器或十六进制，默认 `#1890ff`
   - **排序**（可选）：整数；**新建时界面默认即为 `0`**（与截图一致）。若需区分多条记录在列表中的先后顺序，可改为其他数字；未改则保持 `0` 保存即可
5. 点击「**保存**」

**效果**：新分类出现在表格中（列：**分类名称 / 类型 / 描述 / 颜色 / 排序 / 操作**）。可在行末使用 **编辑**（铅笔图标）、**删除**（垃圾桶图标；删除前会弹出确认）。

> **说明**：本页维护的是企业维度的「客户分类」主数据；若需在客户档案中关联具体分类，请以客户新建/编辑表单中的相关字段为准（以当前版本界面为准）。

### 2.3 创建商机

**场景**：王老板表示下个月要批量采购100箱饮料

**操作步骤**：
1. 点击左侧菜单「客户管理」
2. 在客户管理首页，点击「商机管理」卡片
3. 点击右上角「新增商机」按钮，在弹窗中填写（与当前前端表单一致）：
   - **关联客户**（必填）：须从下拉框选择已有客户（例如刚创建的 `王老板`）；未选择客户无法保存
   - **商机名称**（必填）：`批量采购饮料`
   - **描述**（可选）：如 `100箱饮料，下月提货`
   - **状态**（可选）：默认「潜在」；可选「潜在 / 意向 / 报价 / 成交 / 售后 / 流失」
   - **预计金额**、**实际金额**（可选）：如预计金额填 `5000`
   - **预计成交日期**、**实际成交日期**（可选）：日期选择器，例如预计成交 `2026-06-15`
   - **成交概率 (%)**（可选）：`0`～`100`，默认 `0`，可按判断自行填写
   - **负责人（用户 ID，可选）**：与「归属销售 ID」相同，填企业成员的**用户 ID**（见「用户资料」）；不填表示暂不指定负责人
   - **备注**（可选）
4. 点击「保存」

**效果**：商机创建成功并出现在列表中，可查看状态、预计金额、概率、预计成交日、负责人等。

**进入详情**：在列表中**点击商机名称**，或点击该行末尾 **文档图标**，打开 `/dashboard/crm/opportunities/{id}`，可查看完整字段并使用 **§2.4 AI 智能话术推荐**。

> **💡 编辑商机时**：为与后端接口一致，**不可在弹窗中更换关联客户**；负责人、金额、日期、概率等可在编辑时修改，清空部分字段会同步清空服务端对应数据。**成交概率**当前为表单内人工填写；一键式「AI 成单预测」详见 **§6.1**（Web 端若有入口以实际为准）。

### 2.4 AI智能话术推荐

**场景**：不知道如何跟进王老板

**操作步骤**：
1. 点击左侧菜单「客户管理」→「商机管理」
2. 在商机列表中**点击商机名称**，或点击行末**文档图标**，进入详情页（地址形如：`/dashboard/crm/opportunities/{商机ID}`；若从 §2.3 刚建商机，可直接选该条进入）
3. 在详情页「AI 智能话术推荐」区域，点击 **「AI 话术推荐」** 打开弹窗
4. 在「场景」中选择 **`谈判协商`**（与后端「价格谈判」话术模板对应），点击 **「生成话术」**
5. 查看系统生成的多条话术（含正文、技巧与备选表述），可按场景切换后再次生成

> **说明**：话术由后端模板结合客户与近期跟进、商机记录拼装而成，便于销售参考；与聊天式大模型生成不同。若客户不属于当前企业或 ID 无效，列表可能为空。

**示例话术（示意）**：实际展示以系统生成为准，风格接近价值锚定 / 议价类模板，例如：
> 理解您对价格的考虑，在确认采购量的前提下可讨论优惠方案与合同周期……

### 2.5 跟进记录

**场景**：记录与王老板的沟通内容

**操作步骤**：
1. 点击左侧菜单「客户管理」
2. 在客户管理首页，点击「跟进记录」卡片
3. 在页面顶部 **「选择客户」** 下拉框中选中 `王老板`（须为先前已创建的客户）
4. 点击 **「新增跟进」**，在弹窗中填写：
   - **类型**：默认「文本」；也可选图片 / 语音 / 文件（与列表展示类型一致）
   - **内容**（必填）：如 `已沟通批量采购事宜，客户表示感兴趣`
   - **下次跟进时间**（可选）：使用日期时间选择，如 `2026-05-12` 的合适时段
5. 点击「保存」

> **说明**：当前页为「先选客户、再维护该客户下多条跟进」；未选客户时列表区域会提示先选择客户。

### 2.6 客户流失预警

**场景**：系统自动检测潜在流失客户

**操作步骤**（**目标流程**；Web 端菜单与列表以实际发布的「AI 智能」模块为准）：
1. 点击左侧菜单「AI 智能」
2. 在 AI 智能首页查找「客户流失预警」或同类入口
3. 查看预警列表（示例数据仅供理解业务，非您环境真实数据）
4. 按页面提供的「查看详情」「生成挽留建议」等操作继续

> **说明**：后端可能提供流失预警相关能力；若当前前端未完成聚合页，请以接口文档或后续版本更新为准。

---

## 第三章：进销存管理

### 3.0 新建供应商（主数据）

**场景**：小明便利店要向「旺旺食品」长期进货，需要先在系统里登记供应商，后续在「采购订单」中从下拉框选用（接口字段为 `supplierId`）。

**操作步骤**（与当前 Web **进销存 → 供应商管理** 及后端 `CreateSupplierDto` 一致）：
1. 点击左侧菜单「进销存」
2. 在进销存首页，点击「**供应商管理**」卡片（路径形如：`/dashboard/inventory/suppliers`）
3. 点击右上角「**+ 新建供应商**」
4. 在「**新建供应商**」表单中填写（可与上文 **「模拟数据速查」** 表中供应商 A 一致）：
   - **供应商名称**（必填）：如 `旺旺食品`（界面占位示例亦可填：`杭州某某五金批发`）
   - **联系人**（可选）：如 `张三`
   - **联系电话**（可选）：如 `13900001111`
   - **地址**（可选）：如 `杭州市余杭区某某路100号`
   - **状态**（可选）：默认「**正常**」（对应后端状态值 `active`）；也可选「**停用**」（`inactive`）
5. 点击「**创建**」

**效果**：列表中出现该供应商；打开「采购订单 → 新建采购订单」时，**供应商**下拉框中可选中本条。

> **说明**：教程早期版本未写本节，是因为第三章偏重「一包到底」的故事线，**把供应商一笔带过**；实际上进销存里 **供应商、商品** 与 CRM 里客户一样，属于要先维护的**主数据**。没有单独小节时，就容易出现「界面上有表单、文档里却没有逐步实例」的落差。

### 3.1 新建商品（SKU，主数据）

**场景**：要在「采购订单」的商品行里选到「可乐」，需先在 **商品管理** 中建好该 SKU。采购明细提交的是 **`productId`**，界面以下拉框展示 **商品名称、编码**（与列表数据一致）。

**操作步骤**（与当前 Web **进销存 → 商品管理** 及创建接口字段一致；表单未展示项如 `categoryId`、`status` 由后端默认，可按默认使用）：
1. 点击左侧菜单「进销存」
2. 在进销存首页，点击「**商品管理**」卡片（路径形如：`/dashboard/inventory/products`）
3. 点击「**+ 新建商品**」
4. 在表单中填写——**示例 A（可乐，与 §3.2、§3.3 衔接）**：

| 字段 | 填写示例 |
|------|-----------|
| 商品编码 | `SKU-COLA` |
| 商品名称 | `可乐` |
| 规格 | `500ml` |
| 单位 | `箱`（若按瓶统计可改为 `件`） |
| 销售单价 | `55`（须 **> 0**，否则当前前端会提示必填校验） |
| 成本价 | `40` |
| 最低库存 | `20` |
| 最高库存 | `500` |

**示例 B（与「新建商品」界面占位一致，可选练手）**：编码 `P001`，名称 `六角螺栓 M8×30`，规格 `M8×30 8.8级`，单位 `件`，销售单价 `12.5`，成本价 `8`，最低/最高库存按需或填 `0`。

5. 点击「**创建**」

**效果**：商品出现在列表中；打开「**采购订单 → 新建采购订单 → 添加商品行**」时，在「商品」下拉里可选中本条（行内再填本次采购的数量、单价）。

> **与采购行对齐**：一行明细对应后端 `items[]` 中的 `{ productId, quantity, unitPrice }`，其中 **`productId` 即本 SKU 在系统中的 id**；名称、编码仅作展示，不以文本形式提交。

### 3.2 采购订单：建单、提交、审批与入库（与当前后端一致）

**场景**：从供应商「旺旺食品」采购 50 箱可乐（**请先完成 §3.0 建好该供应商、§3.1 建好「可乐」SKU**）。

本小节按 **「状态机 + 页面按钮」** 说明，避免把「创建」与「入库」混为一谈：**点「创建」只会得到草稿；真正加库存发生在「已批准」之后的入库环节**（见下文「入库与库存」）。

#### 3.2.1 新建采购单（草稿）

**路径**：进销存 → **采购订单**（约 `/dashboard/inventory/purchase-orders`）→ **+ 新建采购订单**。

**操作步骤**：
1. 点击左侧菜单「进销存」
2. 点击「**采购订单**」卡片进入列表
3. 点击「**+ 新建采购订单**」
4. 填写表单（数值可照 **文首「模拟数据速查」** 中「采购单行」一行抄写）：
   - **供应商**（必选）：下拉选择 `旺旺食品`（提交到后端的字段为 **`supplierId`**，对应选项里的 `#id`，无需手填）
   - **采购明细**：「添加商品行」后，选择商品 `可乐`，数量 `50`，单价 `45`（单价允许为 `0`；行数据结构为 **`productId`、`quantity`、`unitPrice`**）
   - **备注**（可选）：如 `首批铺货`（对应后端 **`remark`**）
5. 点击「**创建**」

**结果**：后端会生成单号（如 `PO` 开头）、**状态为「草稿」**（`draft`）。此时**尚未增加库存**，也**未进入审批队列**。

> **编辑草稿**：列表中 **草稿** 行可点「**编辑**」。当前 Web 与后端约定为：**编辑时一般只更新备注**；供应商与明细行已在建单时确定，若需大改，更稳妥的做法是 **删除该草稿后重新建单**（以您当前界面提示为准）。

#### 3.2.2 状态一览（与列表筛选项一致）

| 界面显示 | 后端状态值 | 含义（业务上） |
|----------|------------|----------------|
| 草稿 | `draft` | 可编辑备注、可删除、可「提交」送审 |
| 待审批 | `pending` | 已提交，等待「批准」或「拒绝」 |
| 已批准 | `approved` | 审批通过；**满足条件时可执行入库**（部分入库后单据可仍为已批准，直至收齐） |
| 已完成 | `completed` | 各明细行已全部收齐（`received` 达到订购数量），采购闭环结束 |
| 已取消 | `cancelled` | 审批拒绝或业务作废后的终态之一 |

列表顶部的 **「全部状态」** 下拉框与上表一一对应，便于按状态筛选。

#### 3.2.3 列表上的按钮分别做什么（当前 Web）

以下描述与 **采购订单列表行内按钮** 一致（若您本地界面有迭代，以实际为准）：

| 按钮 | 何时出现 | 作用（与后端行为对应） |
|------|----------|-------------------------|
| **编辑** | 仅 **草稿** | 修改备注等（见 §3.2.1 说明） |
| **提交** | 仅 **草稿** | 将状态从 **`draft` → `pending`（待审批）**，表示「正式送入审批流程」；**不是入库、也不会自动加库存** |
| **删除** | 仅 **草稿** | 物理删除该单及明细（后端限制：非草稿不可删） |
| **批准** | **待审批** | **`pending` → `approved`**，采购单可被入库 |
| **拒绝** | **待审批** | **`pending` → `cancelled`**（视为审批不通过） |
| **取消** | **待审批** 或 **已批准** | 作废单据（后端不允许对 **已完成 / 已取消** 再次作废） |

**小结**：**「提交」= 从草稿正式进入「待审批」**；批准后才会走到可执行入库的 **「已批准」** 状态。

#### 3.2.4 入库与库存（批准后）

**业务含义**：货到仓之后，需要将实收数量记进系统：**只有状态为「已批准」 (`approved`) 的采购单，后端才允许执行入库**。支持按采购明细 **分行、分多次入库**（部分到货）；当**每一行**的已累计实收数量 **等于** 该行订购数量时，整单变为 **「已完成」** (`completed`)。全部收齐后，系统**可能触发采购相关自动记账**（记账失败通常**不阻断**入库结果，具体以服务端配置与日志为准）。

**接口**（便于联调或对账）：`PUT /api/v1/inventory/purchase-orders/:id/inbound`，请求体为 **`{ "items": [ { "itemId": <采购明细 id>, "inboundQty": <本次入库数量> }, ... ] }`**（camelCase，与后端 DTO 一致）。

> **说明（Web 界面）**：仓库中已为上述入库能力提供前端 API 封装，但 **采购订单列表页未必展示「入库」按钮**。若您在浏览器里走完「提交 → 批准」后**看不到入库入口**，需要依赖 **接口文档（如 `docs/api-documentation.md`）**、自建操作入口或后续版本的界面更新；**库存是否增加，以是否在已批准状态下成功执行入库为准**。
>
> 「扫码入库」等若为教学场景或非本仓库已实现能力，请以实际上线菜单为准。

#### 3.2.5 本节练习小结

按顺序完成：**新建供应商与商品 → 新建采购订单（草稿）→ 提交（待审批）→ 批准（已批准）→（如环境支持）入库至完成**。若仅练习到「已批准」，也已完成主流程理解与列表操作。

**效果**：在成功完成 **已全部收齐的入库** 后，对应 SKU 库存增加；价税层面的财务凭证是否自动生成，参见 §3.2.4「自动记账」说明及财务模块文档。

### 3.3 创建销售单

**场景**：王老板来店里买10箱可乐

**模拟单据明细（抄写用）**

| 字段 | 示例值 |
|------|--------|
| 客户 | `王老板`（须已在 §2.1 创建） |
| 商品 | `可乐`（须已在 §3.1 创建） |
| 数量 | `10` |
| 单价 | `55`（若与您设置的 VIP/会员价策略一致，按界面为准） |

**操作步骤**：
1. 点击左侧菜单「进销存」
2. 在进销存首页，点击「销售订单」卡片
3. 进入销售订单页面后，点击右上角「新建销售单」按钮
4. 选择客户：`王老板`
5. 添加商品（与上表一致）：
   - 商品：`可乐`
   - 数量：`10`
   - 单价：`55`
6. 点击「保存并出库」

**效果**（理想闭环；是否自动生成凭证、是否联动会员积分**取决于当前版本配置与业务流程是否已打通**）：
- 库存减少 10 箱
- 可能自动生成财务凭证（请至「财务 → 凭证管理」核对）
- 若已启用会员积分规则，可能增加客户积分

### 3.4 多仓库管理

本节分两部分：**先在本环境中真实建档仓库**（与列表、盘点下拉中的 `warehouseId` 对齐），再以表格理解「多仓分布」的教学示意。

#### 3.4.1 仓库管理：新建仓库档案（当前 Web）

**路径**：`/dashboard/inventory/warehouses`（进销存首页 → 「**仓库管理**」卡片）

**后端约定（便于核对）**：列表 **`GET /api/v1/inventory/warehouses`**；新建 **`POST /api/v1/inventory/warehouses`**，请求体字段与表单一致：**`name`（必填）**、`code`、`address`、`phone`、`remark`、`isDefault`（可选）。

**场景 A：企业尚无仓库——先建「主仓库」**

若页面提示尚无档案，或盘点里仓库下拉为空，可先按本节建一条默认仓。

**操作步骤**：
1. 点击左侧菜单「进销存」
2. 在进销存首页，点击「**仓库管理**」卡片
3. 点击右上角「**新建仓库**」，在弹窗中填写（抄写可用文首 **速查表「仓库主仓库」** 一行）：
   - **仓库名称**（必填）：`主仓库`（占位符示例：「例如：主仓库」）
   - **编码**（可选）：如 `WH-MAIN`（内部编号，可不填）
   - **地址 / 联系电话 / 备注**（可选）：可按「小明便利店」故事自拟，或与速查表中总店示意一致
   - **设为默认仓库**：**勾选**——同一企业仅建议保留一个默认；首次建档时勾选，便于盘点、出库等下拉默认选中
4. 点击「**创建**」

**结果**：对话框关闭后列表刷新，出现该行；后续在「**库存盘点**」中建单时，「仓库」下拉里可出现本条（值为后端返回的 **`id`**，即系统中的 `warehouseId`）。

**说明**：
- 若您更习惯脚本灌数，可在 `uxyy-api` 目录执行 **`pnpm db:seed`**（会写入演示用「主仓库」等）；与界面「新建仓库」二选一或并存均可，注意不要重复同名导致后端提示「仓库名称已存在」。
- 表单未开放「负责人 / `managerId`」项时由后端空置即可，不影响名称与默认仓逻辑。

**场景 B（练习）：再加一个非默认分店仓**

在同一页再次「新建仓库」：**名称** `分店A`，**编码** 可选 `WH-A`，**不设为默认**。保存后列表应有两条，其中仅一条标记为默认。

---

#### 3.4.2 各仓库存一览（教学示意）

**场景**：需要从故事上理解「多仓各有多少货」——**下列为教学模拟数据**，与真实列表是否分页、是否按 SKU 分列展示以您当前盘点/库存页为准。

**操作步骤（示意流程）**：
1. 点击左侧菜单「进销存」
2. 在进销存首页，点击「**库存盘点**」卡片（或「库存明细」等，**以侧边栏卡片为准**）
3. 对照下图景理解（非必然与库里真实数量一致）：

   | 仓库 | 可乐 | 矿泉水 | 零食 |
   |------|------|--------|------|
   | 总店 | 40箱 | 60箱 | 100袋 |
   | 分店A | 20箱 | 30箱 | 50袋 |
   | 分店B | 30箱 | 45箱 | 75袋 |

4. 若有「调拨」等菜单或按钮，按其说明在**已上线的流程**中操作；本节仅为业务故事线示意。

### 3.5 库存预警

**场景**：系统提醒库存不足

**操作步骤**：
1. 登录首页查看预警信息
2. 点击「库存预警」卡片查看详情（**示例**）：
   - 矿泉水：库存15箱，低于下限20箱
3. 点击「生成采购建议」
4. 系统自动生成采购单

---

## 第四章：财务管理

### 4.1 发票录入

**场景**：收到供应商开具的增值税发票

**操作步骤**：
1. 点击左侧菜单「财务」
2. 在财务首页，点击「发票管理」卡片
3. 进入发票管理页面后，点击右上角「录入发票」按钮
4. 选择方式：
   - **OCR识别**：上传发票图片（推荐）
   - **手动录入**：填写发票信息
5. 发票信息（可与文首 **「模拟数据速查」** 财务区对照）：
   - 金额：`2250`
   - 税额：`292.5`
   - 价税合计：`2542.5`
6. 关联采购单（可选）
7. 点击「保存」

**效果**：发票自动验证，状态变为「已验证」

### 4.2 自动记账

**场景**：系统自动生成财务凭证

**操作步骤**：
1. 发票录入成功后，系统自动生成凭证
2. 点击左侧菜单「财务」→「凭证管理」卡片查看
3. 凭证信息：
   - 凭证号：`V202605080001`
   - 借方：库存商品 2250
   - 贷方：应付账款 2542.5
   - 贷方：应交税费 292.5

### 4.3 AI智能纠错

**场景**：系统检测凭证错误

**操作步骤**：
1. 点击左侧菜单「财务」
2. 在财务首页，点击「AI智能纠错」卡片
3. 选择日期范围：`2026-05-01` 至 `2026-05-31`
4. 点击「检测」
5. 查看检测结果：
   - 凭证V202605050003：借贷不平衡
   - 凭证V202605060008：科目选择错误
6. 点击「自动修复」

### 4.4 税务申报数据准备

**场景**：准备5月份税务申报数据

**操作步骤**：
1. 点击左侧菜单「财务」
2. 在财务首页，点击「税务申报」卡片
3. 选择所属期：`2026-05`
4. 点击「生成报告」
5. 查看数据：
   - 增值税：销项6500元，进项3900元，应缴2600元
   - 企业所得税：利润20000元，应缴1000元
6. 点击「导出Excel」或「导出PDF」
7. 用户自行登录电子税务局上传申报

---

## 第五章：OA办公

### 5.1 考勤打卡

**场景**：员工小张上班打卡

**操作步骤**：
1. 点击左侧菜单「OA办公」
2. 在OA办公首页，点击「考勤管理」卡片
3. 进入考勤管理页面后，点击「上班打卡」
4. 系统自动记录：
   - 时间：`2026-05-08 08:55`
   - 地点：小明便利店总店

### 5.2 请假申请

**场景**：员工小李需要请假2天

**操作步骤**：
1. 点击左侧菜单「OA办公」
2. 在OA办公首页，点击「请假管理」卡片
3. 进入请假管理页面后，点击右上角「新建申请」按钮
4. 填写信息：
   - 类型：`事假`
   - 开始日期：`2026-05-10`
   - 结束日期：`2026-05-11`
   - 原因：`家中有事`
5. 点击「提交」

**效果**：审批流程自动发送给上级

### 5.3 审批流程

**场景**：老板审批请假申请

**操作步骤**：
1. 登录系统查看消息提醒（右上角铃铛图标）
2. 点击左侧菜单「OA办公」
3. 在OA办公首页，点击「审批中心」卡片
4. 选择待审批的请假申请
5. 点击「同意」或「拒绝」
6. 填写审批意见

**效果**：申请人收到审批结果通知

### 5.4 报销申请

**场景**：员工小王报销差旅费

**操作步骤**：
1. 点击左侧菜单「OA办公」
2. 在OA办公首页，点击「报销管理」卡片
3. 进入报销管理页面后，点击右上角「新建报销」按钮
4. 填写信息：
   - 类型：`差旅费`
   - 金额：`500`
   - 上传凭证图片（OCR自动识别金额）
5. 点击「提交」

---

## 第六章：AI智能功能

### 6.1 商机成单预测

**场景**：评估王老板相关商机的成单可能性

**当前 Web（MVP）常用方式**：
1. 客户管理 → **商机管理**，在新建 / 编辑商机时填写 **「成交概率 (%)」**（0～100），用于列表与详情展示
2. 进入 **商机详情页**（见 §2.3 / §2.4）可结合 **§2.4 AI 话术推荐** 辅助沟通策略

**独立「AI 预测」入口**（示例流程；后端可能存在预测类 API，但 **当前商机详情页未内置单独的「AI 预测」按钮**，请勿与「AI 话术推荐」混淆）：
1. 客户管理 → 商机管理 → 进入某商机详情
2. 若未来版本提供「AI 预测」或 AI 模块内专题页，可按页面指引查看例如：
   - 成单概率建议值、风险等级、跟进建议文案等（**以下为示意**）
   - 成单概率：`78%` · 风险：`低` · 建议：`本周内再次跟进，可酌情提供小幅优惠以促成签约` 等

> **说明**：请以实际上线的 AI 菜单与商机页按钮为准；教程数据仅为教学示例。

### 6.2 客户流失预警

**场景**：识别高风险流失客户

**操作步骤**：与 **§2.6** 类似，从「AI 智能」进入相关能力；下列表格为**教学示意**，非真实运行结果。

| 客户 | 未下单天数 | 风险等级 | 建议 |
|------|----------|---------|------|
| 李老板 | 45天 | 高 | 立即联系，提供专属优惠 |
| 张老板 | 20天 | 中 | 发送新品推荐短信 |

### 6.3 智能采购建议

**场景**：结合库存与销售情况给出补货参考

**操作步骤**（菜单名称以进销存首页实际卡片为准）：
1. 点击「进销存」
2. 查找「采购建议」或同类分析入口
3. 参考列表中的商品、库存与建议量（下表示意）：

| 商品 | 当前库存 | 建议采购量 | 理由 |
|------|---------|-----------|------|
| 矿泉水 | 15箱 | 30箱 | 库存低于预警线 |
| 薯片 | 80袋 | 20袋 | 预计下周销售需求 |

---

## 第七章：数据导出

### 7.1 导出销售报表

**操作步骤**：
1. 点击左侧菜单「财务」
2. 在财务首页，点击「报表统计」卡片
3. 选择时间范围：`2026-05-01` 至 `2026-05-31`
4. 点击「导出Excel」

### 7.2 导出PDF单据

**操作步骤**：
1. 点击左侧菜单「进销存」→「**销售订单**」卡片（以实际菜单为准）
2. 在销售单列表中选择目标单据进入详情页
3. 点击「打印」→「导出PDF」
4. 下载销售单PDF文件

---

## 📝 练习任务

> **提示**：下列步骤中的名称、编码、金额可与文首 **「📋 模拟数据与实例速查」** 表对照填写；若提示「已存在」，可在同一表规律下略改编码或名称。

### 任务1：完整销售流程
1. 点击「客户管理」→「客户列表」→「新建」，创建客户「刘老板」（电话等可按速查表中王老板同款或自拟）
2. 确保进销存中已有可售商品（建议按 **§3.1 示例 A** 或速查表建「550ml 矿泉水」SKU：`SKU-WATER-550`、单价 `36` 等）
3. 点击「进销存」→「**销售订单**」→「新建」（或「+ 新建销售单」，**以按钮文案为准**），向「刘老板」销售 20 箱矿泉水，单价可按 `36` 或与商品档案一致
4. 如需先保证有仓库下拉项，可先按 **§3.4.1** 进入「仓库管理」新建仓库；随后在「**商品管理**」或「**库存盘点**」等菜单中核对库存变化（**以您侧边栏实际卡片为准**）
5. 点击「财务」→「凭证管理」查看是否生成凭证（**取决于当前版本是否已打通业务→财务**）

### 任务2：采购补货流程
1. 点击「进销存」→「**供应商管理**」→「+ 新建供应商」，创建「农夫山泉」（字段同 **§3.0**；电话、地址可照速查表中旺旺食品格式自拟）
2. 点击「进销存」→「**商品管理**」→「+ 新建商品」，为后续采购准备 SKU（字段同 **§3.1**；例如编码 `SKU-WATER-550`、名称 `550ml 矿泉水`、销售单价 `36`、单位 `箱`）
3. 点击「进销存」→「**采购订单**」→「+ 新建采购订单」，选择该供应商并添加商品行（如矿泉水 50 箱），点击「创建」得到 **草稿**；再按列表上的 **提交 → 批准** 推进状态（详见 **§3.2**，尤其 **§3.2.3「提交」≠ 入库**）
4. **入库与库存**：若界面无「入库」入口，参见 **§3.2.4**（API 或通过其他环境完成）；完成后在库存相关页面核对
5. 点击「财务」→「发票管理」→「录入发票」，录入采购发票（若模块已启用）

### 任务3：客户分类（与 §2.2 对齐）
1. 「客户管理」→「客户分类」
2. 点击「新增分类」，新增一条示例分类（如名称「教程-批发客户」、类型「自定义」）
3. 在列表中确认出现该行；可尝试编辑后保存，或删除测试数据

### 任务4：商机与 AI 话术（与 §2.3、§2.4 对齐）
1. 「客户管理」→「商机管理」，新建或选择一条商机
2. 点击商机名称进入详情页 `/dashboard/crm/opportunities/{id}`（步骤同 §2.3、§2.4）
3. 打开「AI 话术推荐」，场景选「谈判协商」，生成并阅读话术

---

## 🎓 学习路径建议

| 阶段 | 时间 | 学习内容 |
|------|------|---------|
| 第一天 | 1小时 | 注册登录、客户列表、**客户分类**、商机列表、商机详情与 AI 话术 |
| 第二天 | 1小时 | 进销存销售流程 |
| 第三天 | 1小时 | **§3.0 供应商**、**§3.1 商品 SKU**、**§3.4.1 仓库档案**、采购订单与销售订单（见第三章） |
| 第四天 | 1小时 | 财务发票与记账 |
| 第五天 | 1小时 | OA考勤与审批 |
| 第六天 | 1小时 | AI智能功能 |
| 第七天 | 1小时 | 综合练习 |

---

## 💡 快速技巧

1. **快捷键 / 全局搜索**：若产品在后续版本中提供，请以设置页或帮助文档为准；当前 MVP 不保证以下快捷键已启用：
   - `Ctrl + N`：新建销售单（示例）
   - `Ctrl + F`：全局搜索（示例）
   - `Ctrl + S`：快速保存（示例）

2. **移动端**：小程序等能力以官方发布为准。

3. **帮助中心**：若导航栏提供「帮助」入口，可跳转站内文档；亦可对照仓库内 `docs`、`PRD` 与 OpenAPI（`/docs`）。

---

> 🎉 恭喜您完成优效营系统操作教程！如需进一步了解某个功能，请查看系统内帮助文档或联系客服。

**优效营 - 让小微企业经营更高效**  
[官网](https://uxyy.cn) | [帮助中心](https://uxyy.cn/help) | [客服支持](https://uxyy.cn/support)