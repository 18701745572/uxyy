# 优效营 Monorepo · 环境变量示意
#
# 后端运行时请在 uxyy-api 目录使用独立 .env：
#   复制 uxyy-api/.env.example → uxyy-api/.env
#
# 本地 Postgres（与仓库根 docker-compose.yml 默认一致）
#   仓库根目录：docker compose up -d postgres
#
DATABASE_URL=postgresql://postgres:postgres@127.0.0.1:5432/uxyy
