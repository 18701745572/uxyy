# ── 本地端口（与后端对齐）────────────────────────────────────────────
#   浏览器打开页面：http://localhost:3001 （pnpm dev 固定端口，见 package.json）
#   接口请求发往：NEXT_PUBLIC_API_URL（须与 uxyy-api 的 PORT 一致，本地一般为 3000）
#   E2E（pnpm dev:e2e）：Playwright 连 3101，API 仍建议 3000（由 playwright.config 传入）
#
# 浏览器请求 Nest API 的根 URL（末尾不要斜杠）。
# 本地：http://127.0.0.1:3000  生产：https://api.example.com
NEXT_PUBLIC_API_URL=http://127.0.0.1:3000

# Playwright E2E（可选）：与 uxyy-api db:seed 默认一致
# E2E_API_URL=http://localhost:3000
# E2E_PHONE=13800138000
# E2E_PASSWORD=Dev12345!
# PLAYWRIGHT_BASE_URL=http://localhost:3101
# 默认不设置：登录后用例会 mock /api/v1，无需起 Nest。设为 1 时对真实 API 做 login + health 校验。
# E2E_LIVE_API=1

# 说明：历史上曾有 NEXT_PUBLIC_USE_MOCK；已删除 MSW，请勿再配置该变量。
