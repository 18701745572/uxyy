# 浏览器请求 Nest API 的根 URL（末尾不要斜杠）。
#   示例：前端 3001、后端 3000 时互不占用端口；或同源反代时再改。
NEXT_PUBLIC_API_URL=http://127.0.0.1:3000

# 设为 true 时启用 MSW Mock（无需后端即可开发 UI）。
#   默认 false，连真实 API。
NEXT_PUBLIC_USE_MOCK=false
